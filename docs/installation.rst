============
Installation
============

At the command line::

    $ easy_install django-tables2-actionscolumn

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-tables2-actionscolumn
    $ pip install django-tables2-actionscolumn